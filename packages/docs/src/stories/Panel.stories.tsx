import { PanelTest } from '@alexandria-ui/design-system'
export default {
  title: 'Design System/Panel',
  component: PanelTest,
}

export const Panel = (args: any) => {
  return <PanelTest {...args} />
}
